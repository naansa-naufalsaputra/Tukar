{
  "name": "gemini-router",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Secure local credential router for Gemini failover",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check providers.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
